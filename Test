Yes. The error happens because your code and the stored procedure parameters do not match.

The safest way to fix it is to make sure the parameters in your C# code are exactly the same as the parameters expected by MA_InsertAttendee.

Below are the recommended steps.


---

1️⃣ Check the Stored Procedure Parameters

Run this in SQL Server:

sp_help MA_InsertAttendee

or

EXEC sp_helptext 'MA_InsertAttendee'

Example output might show:

CREATE PROCEDURE MA_InsertAttendee
    @MeetingId INT,
    @CardSerialNo NVARCHAR(50)

or

CREATE PROCEDURE MA_InsertAttendee
    @MeetingId INT,
    @EmployeeNo NVARCHAR(50)

This tells you what parameters the procedure expects.


---

2️⃣ Match the Parameters in C# Code

If the procedure expects @CardSerialNo

Change your code to:

insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
insertCmd.Parameters.AddWithValue("@CardSerialNo", cardSerial);


---

If the procedure expects @EmployeeNo

Then your current code is correct:

insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

But you must also update the stored procedure.

Example:

ALTER PROCEDURE MA_InsertAttendee
    @MeetingId INT,
    @EmployeeNo NVARCHAR(50)
AS
BEGIN
    INSERT INTO MeetingAttendance
    (
        MeetingId,
        EmployeeNo,
        TimeIn
    )
    VALUES
    (
        @MeetingId,
        @EmployeeNo,
        GETDATE()
    )

    SELECT 1 AS Result
END


---

3️⃣ Best Fix for Your System (Recommended)

Since your system now uses EmployeeRecord.cs, the better design is:

Stored procedure parameters:

@MeetingId
@EmployeeNo

Then your code remains:

insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

This is cleaner and more future-proof.


---

4️⃣ Quick Debug Tip

If you want to quickly see the missing parameter, wrap your insert code like this:

foreach (SqlParameter p in insertCmd.Parameters)
{
    System.Diagnostics.Debug.WriteLine(p.ParameterName);
}

But usually the SQL error message already tells the missing parameter name.

Example:

Procedure or function MA_InsertAttendee expects parameter '@CardSerialNo', which was not supplied.


---

✅ My recommendation for your meeting attendance system:

Keep these parameters:

@MeetingId
@EmployeeNo

and update the stored procedure to use EmployeeNo instead of CardSerialNo.

This will make the system independent from the card hardware.


---

💡 If you want, paste your MA_InsertAttendee stored procedure, and I can fix it for you in 1 clean version compatible with your new EmployeeRecord logic.



*****


The error:

Procedure or function 'MA_InsertAttendee' expects parameter '@____', which was not supplied

means the stored procedure requires a parameter that your code did not pass.

Earlier your system probably used:

@MeetingId
@CardSerialNo

But now your code sends:

@MeetingId
@EmployeeNo

So if the stored procedure still expects @CardSerialNo, SQL Server will throw this error.


---

Step 1 — Check the Stored Procedure

Run this in SQL Server:

sp_help MA_InsertAttendee

or

EXEC sp_helptext 'MA_InsertAttendee'

Example result might show:

CREATE PROCEDURE MA_InsertAttendee
    @MeetingId INT,
    @CardSerialNo NVARCHAR(50)
AS

If this is the case, your code must send:

insertCmd.Parameters.AddWithValue("@CardSerialNo", cardSerial);

NOT @EmployeeNo.


---

Solution Option 1 (Recommended)

If your attendance table now uses EmployeeNo, update the stored procedure.

Example:

ALTER PROCEDURE MA_InsertAttendee
    @MeetingId INT,
    @EmployeeNo NVARCHAR(50)
AS
BEGIN
    INSERT INTO MeetingAttendance
    (
        MeetingId,
        EmployeeNo,
        TimeIn
    )
    VALUES
    (
        @MeetingId,
        @EmployeeNo,
        GETDATE()
    )

    SELECT 1 AS Result
END

Then your code is correct:

insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);


---

Solution Option 2 (Quick Fix)

If you cannot change the stored procedure, keep using CardSerialNo.

insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
insertCmd.Parameters.AddWithValue("@CardSerialNo", cardSerial);

But still use EmployeeRecord only for name and department display.


---

How to Know Exactly Which Parameter is Missing

The full error usually says something like:

Procedure or function 'MA_InsertAttendee' expects parameter '@CardSerialNo', which was not supplied

or

expects parameter '@EmployeeNo'

That name tells you what the stored procedure requires.


---

Most Likely Cause in Your Case

Since your original code used CardSerialNo, the procedure probably still expects it.

So change back to:

insertCmd.Parameters.AddWithValue("@CardSerialNo", cardSerial);

instead of

insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);


---

Best Long-Term Design (Recommended for your system)

Use both:

MeetingId
EmployeeNo
CardSerialNo
TimeIn

This makes the attendance traceable and more secure.


---

✅ If you want, you can paste your MA_InsertAttendee stored procedure, and I can show you exactly what parameter your code must pass and also improve the procedure for your meeting attendance system.




*****



Yes, you can implement it like this:

Logic you want:

Tap Card
   ↓
Check EmployeeRecord.cs
   ↓
Found?
 ├─ YES → Use EmployeeNo → InsertAttendee / UpdateTimeout
 └─ NO  → Check MA_CheckEmployeeByCard
            ↓
         Found?
         ├─ YES → InsertAttendee / UpdateTimeout
         └─ NO  → Card Not Registered

So both sources can insert/update attendance, but EmployeeRecord is prioritized.

Below is the complete updated code implementing this logic.


---

Complete Updated Code

protected void txtCardSerial_TextChanged(object sender, EventArgs e)
{
    string cardSerial = txtCardSerial.Text.Trim();
    string meetingId = hfMeetingID.Value;

    string tapMode = Request.Form["tapMode"];
    if (string.IsNullOrEmpty(tapMode))
        tapMode = hfTapMode.Value ?? "In";

    if (string.IsNullOrEmpty(cardSerial))
    {
        ShowAlert("Invalid Tap", "Please place your card on the reader.", "", "alert-warning");
        return;
    }

    string connStr = ConfigurationManager.ConnectionStrings["MeetingDb"].ConnectionString;
    string conns = ConfigurationManager.ConnectionStrings["StrConn"].ConnectionString;

    string strEmpNo = "";
    string empName = "";
    string dept = "";

    // FIRST: Check EmployeeRecord.cs
    EmployeeRecord empRec = new EmployeeRecord(conns);
    empRec.GetEmployeeDetailsByEM(cardSerial);

    if (!string.IsNullOrEmpty(empRec.EmployeeNo))
    {
        strEmpNo = empRec.EmployeeNo;

        empName = empRec.FirstName + " " + empRec.MiddleName + " " + empRec.LastName;
        dept = empRec.DivisionName + " " + empRec.DepartmentName + " " + empRec.SectionName;
    }

    try
    {
        using (SqlConnection conn = new SqlConnection(connStr))
        {
            conn.Open();

            // SECOND: If not found in EmployeeRecord, check Meeting DB
            if (string.IsNullOrEmpty(strEmpNo))
            {
                using (SqlCommand cmd = new SqlCommand("MA_CheckEmployeeByCard", conn))
                {
                    cmd.CommandType = CommandType.StoredProcedure;
                    cmd.Parameters.AddWithValue("@CardSerialNo", cardSerial);

                    using (SqlDataReader reader = cmd.ExecuteReader())
                    {
                        if (!reader.Read())
                        {
                            txtCardSerial.Text = "";
                            ShowAlert("Card Not Registered", "", "", "alert-danger");
                            return;
                        }

                        strEmpNo = (reader["EmployeeNo"] ?? "").ToString().Trim();

                        empName = (reader["FirstName"] ?? "").ToString().Trim() + " " +
                                  (reader["MiddleName"] ?? "").ToString().Trim() + " " +
                                  (reader["LastName"] ?? "").ToString().Trim();

                        dept = (reader["DivisionName"] ?? "").ToString().Trim() + " " +
                               (reader["DepartmentName"] ?? "").ToString().Trim() + " " +
                               (reader["SectionName"] ?? "").ToString().Trim();
                    }
                }
            }

            // TAP IN
            if (tapMode.Equals("In", StringComparison.OrdinalIgnoreCase))
            {
                using (SqlCommand insertCmd = new SqlCommand("MA_InsertAttendee", conn))
                {
                    insertCmd.CommandType = CommandType.StoredProcedure;

                    insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
                    insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                    using (SqlDataReader reader = insertCmd.ExecuteReader())
                    {
                        if (reader.Read())
                        {
                            int result = Convert.ToInt32(reader["Result"]);

                            if (result == 0)
                                ShowAlert("Already Tapped In", "", "", "alert-danger");
                            else if (result == 1)
                                ShowAlert("You have tapped in successfully!", empName, dept, "alert-success");
                            else
                                ShowAlert("Tap-in failed", "", "", "alert-danger");
                        }
                    }
                }
            }
            // TAP OUT
            else
            {
                using (SqlCommand updateCmd = new SqlCommand("MA_UpdateAttendeeTimeout", conn))
                {
                    updateCmd.CommandType = CommandType.StoredProcedure;

                    updateCmd.Parameters.AddWithValue("@MeetingId", meetingId);
                    updateCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                    int rowsAffected = updateCmd.ExecuteNonQuery();

                    if (rowsAffected > 0)
                        ShowAlert("You have tapped out successfully!", empName, dept, "alert-success");
                    else
                        ShowAlert("Already tapped out or no tap-in found", "", "", "alert-danger");
                }
            }

            txtCardSerial.Text = "";

            ScriptManager.RegisterStartupScript(this, GetType(), "setFocus",
                $"document.getElementById('{txtCardSerial.ClientID}').focus();", true);
        }
    }
    catch (Exception ex)
    {
        ShowAlert("Error", ex.Message, "", "alert-danger");
    }
}


---

Example Scenario

Card Serial: 1028

Case 1 — Exists in EmployeeRecord

EmployeeNo = E1028
Name = Juan D Cruz
Department = Production Assembly

Tap IN

Result:

✔ You have tapped in successfully!

Juan D Cruz
Production Assembly


---

Case 2 — Not in EmployeeRecord but exists in Meeting DB

System automatically checks:

MA_CheckEmployeeByCard

Then continues attendance.


---

Case 3 — Not in both systems

Output:

Card Not Registered


---

Final Architecture (Best Practice)

RFID Card
   ↓
EmployeeRecord.cs  ← Primary Source
   ↓
Meeting DB Check   ← Backup Source
   ↓
InsertAttendee / UpdateTimeout


---

💡 One thing I noticed from your system design (very common issue in RFID attendance systems):

Your code can still experience double tap insertion because RFID readers sometimes send 2 scans within milliseconds.

If you want, I can show you a very simple 3-line protection used in production RFID systems that prevents:

duplicate tap-in

duplicate timeout

double reader trigger.



*****


Yes, that is also possible and it is a good fallback design.

Your system logic will become:

Tap Card
   ↓
Check EmployeeRecord.cs
   ↓
IF found → use EmployeeRecord details
IF not found → check MA_CheckEmployeeByCard
   ↓
Tap In / Tap Out

This means:

Source	Purpose

EmployeeRecord.cs	Primary employee source
MA_CheckEmployeeByCard	Backup source



---

Updated Complete Code (With Fallback)

protected void txtCardSerial_TextChanged(object sender, EventArgs e)
{
    string cardSerial = txtCardSerial.Text.Trim();
    string meetingId = hfMeetingID.Value;

    string tapMode = Request.Form["tapMode"];
    if (string.IsNullOrEmpty(tapMode))
        tapMode = hfTapMode.Value ?? "In";

    if (string.IsNullOrEmpty(cardSerial))
    {
        ShowAlert("Invalid Tap", "Please place your card on the reader.", "", "alert-warning");
        return;
    }

    string connStr = ConfigurationManager.ConnectionStrings["MeetingDb"].ConnectionString;
    string conns = ConfigurationManager.ConnectionStrings["StrConn"].ConnectionString;

    string strEmpNo = "";
    string empName = "";
    string dept = "";

    // FIRST: Check EmployeeRecord.cs
    EmployeeRecord empRec = new EmployeeRecord(conns);
    empRec.GetEmployeeDetailsByEM(cardSerial);

    if (!string.IsNullOrEmpty(empRec.EmployeeNo))
    {
        // Found in EmployeeRecord
        strEmpNo = empRec.EmployeeNo;

        empName = empRec.FirstName + " " + empRec.MiddleName + " " + empRec.LastName;
        dept = empRec.DivisionName + " " + empRec.DepartmentName + " " + empRec.SectionName;
    }

    try
    {
        using (SqlConnection conn = new SqlConnection(connStr))
        {
            conn.Open();

            // SECOND: If not found in EmployeeRecord, check Meeting DB
            if (string.IsNullOrEmpty(strEmpNo))
            {
                using (SqlCommand cmd = new SqlCommand("MA_CheckEmployeeByCard", conn))
                {
                    cmd.CommandType = CommandType.StoredProcedure;
                    cmd.Parameters.AddWithValue("@CardSerialNo", cardSerial);

                    using (SqlDataReader reader = cmd.ExecuteReader())
                    {
                        if (!reader.Read())
                        {
                            txtCardSerial.Text = "";
                            ShowAlert("Card Not Registered", "", "", "alert-danger");
                            return;
                        }

                        strEmpNo = (reader["EmployeeNo"] ?? "").ToString().Trim();

                        empName = (reader["FirstName"] ?? "").ToString().Trim() + " " +
                                  (reader["MiddleName"] ?? "").ToString().Trim() + " " +
                                  (reader["LastName"] ?? "").ToString().Trim();

                        dept = (reader["DivisionName"] ?? "").ToString().Trim() + " " +
                               (reader["DepartmentName"] ?? "").ToString().Trim() + " " +
                               (reader["SectionName"] ?? "").ToString().Trim();
                    }
                }
            }

            if (tapMode.Equals("In", StringComparison.OrdinalIgnoreCase))
            {
                using (SqlCommand insertCmd = new SqlCommand("MA_InsertAttendee", conn))
                {
                    insertCmd.CommandType = CommandType.StoredProcedure;

                    insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
                    insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                    using (SqlDataReader reader = insertCmd.ExecuteReader())
                    {
                        if (reader.Read())
                        {
                            int result = Convert.ToInt32(reader["Result"]);

                            if (result == 0)
                                ShowAlert("Already Tapped In", "", "", "alert-danger");
                            else if (result == 1)
                                ShowAlert("You have tapped in successfully!", empName, dept, "alert-success");
                            else
                                ShowAlert("Tap-in failed", "", "", "alert-danger");
                        }
                    }
                }
            }
            else
            {
                using (SqlCommand updateCmd = new SqlCommand("MA_UpdateAttendeeTimeout", conn))
                {
                    updateCmd.CommandType = CommandType.StoredProcedure;

                    updateCmd.Parameters.AddWithValue("@MeetingId", meetingId);
                    updateCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                    int rowsAffected = updateCmd.ExecuteNonQuery();

                    if (rowsAffected > 0)
                        ShowAlert("You have tapped out successfully!", empName, dept, "alert-success");
                    else
                        ShowAlert("Already tapped out or no tap-in found", "", "", "alert-danger");
                }
            }

            txtCardSerial.Text = "";

            ScriptManager.RegisterStartupScript(this, GetType(), "setFocus",
                $"document.getElementById('{txtCardSerial.ClientID}').focus();", true);
        }
    }
    catch (Exception ex)
    {
        ShowAlert("Error", ex.Message, "", "alert-danger");
    }
}


---

Final Logic (Simple)

Tap Card
   ↓
EmployeeRecord.cs
   ↓
Found?
 ├─ YES → Use EmployeeRecord data
 └─ NO  → Check MA_CheckEmployeeByCard
            ↓
         Found?
         ├─ YES → Continue attendance
         └─ NO  → Card Not Registered


---

Advantage of This Design

✔ Faster (EmployeeRecord checked first)
✔ Backward compatible with existing meeting DB
✔ No attendance disruption if EmployeeRecord is missing data


---

💡 One more improvement I recommend for your system (very useful for RFID tapping systems):

Add duplicate tap protection (1–2 seconds) because card readers often send multiple scans automatically.

If you want, I can show you a very clean enterprise version of this method (about 40% cleaner code) that will make your Meeting Attendance System production-ready.



******


Below is the complete updated version of your txtCardSerial_TextChanged method where:

✅ The system checks employee using EmployeeRecord.cs first
✅ Uses EmployeeNo (strEmpNo) for attendance
✅ Stops the process if the employee is not found in EmployeeRecord
✅ Keeps your existing Tap In / Tap Out logic

I kept the structure very close to your original code so it will be easier for you to review and explain during development.


---

Updated Complete Code

protected void txtCardSerial_TextChanged(object sender, EventArgs e)
{
    string cardSerial = txtCardSerial.Text.Trim();
    string meetingId = hfMeetingID.Value;

    // Prefer real posted radio value; fallback to the hidden field (hfTapMode)
    string tapMode = Request.Form["tapMode"];
    if (string.IsNullOrEmpty(tapMode))
        tapMode = hfTapMode.Value ?? "In";

    if (string.IsNullOrEmpty(cardSerial))
    {
        ShowAlert("Invalid Tap", "Please place your card on the reader.", "", "alert-warning");
        return;
    }

    string connStr = ConfigurationManager.ConnectionStrings["MeetingDb"].ConnectionString;
    string conns = ConfigurationManager.ConnectionStrings["StrConn"].ConnectionString;

    // Get employee information from EmployeeRecord.cs
    EmployeeRecord empRec = new EmployeeRecord(conns);
    empRec.GetEmployeeDetailsByEM(cardSerial);

    string strEmpNo = empRec.EmployeeNo;

    // Validate employee from EmployeeRecord
    if (string.IsNullOrEmpty(strEmpNo))
    {
        txtCardSerial.Text = "";
        ShowAlert("Employee Not Found", "Card not registered in Employee Record.", "", "alert-danger");
        return;
    }

    try
    {
        using (SqlConnection conn = new SqlConnection(connStr))
        {
            conn.Open();

            string empName = "";
            string dept = "";

            // Validate employee in Meeting DB
            using (SqlCommand cmd = new SqlCommand("MA_CheckEmployeeByCard", conn))
            {
                cmd.CommandType = CommandType.StoredProcedure;
                cmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                using (SqlDataReader reader = cmd.ExecuteReader())
                {
                    if (!reader.Read())
                    {
                        txtCardSerial.Text = "";
                        ShowAlert("Employee Not Registered", "", "", "alert-danger");
                        return;
                    }

                    empName = (reader["FirstName"] ?? "").ToString().Trim() + " " +
                              (reader["MiddleName"] ?? "").ToString().Trim() + " " +
                              (reader["LastName"] ?? "").ToString().Trim();

                    dept = (reader["DivisionName"] ?? "").ToString().Trim() + " " +
                           (reader["DepartmentName"] ?? "").ToString().Trim() + " " +
                           (reader["SectionName"] ?? "").ToString().Trim();
                }
            }

            if (tapMode.Equals("In", StringComparison.OrdinalIgnoreCase))
            {
                using (SqlCommand insertCmd = new SqlCommand("MA_InsertAttendee", conn))
                {
                    insertCmd.CommandType = CommandType.StoredProcedure;

                    insertCmd.Parameters.AddWithValue("@MeetingId", meetingId);
                    insertCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                    using (SqlDataReader reader = insertCmd.ExecuteReader())
                    {
                        if (reader.Read())
                        {
                            int result = Convert.ToInt32(reader["Result"]);

                            if (result == 0)
                                ShowAlert("Already Tapped In", "", "", "alert-danger");
                            else if (result == 1)
                                ShowAlert("You have tapped in successfully!", empName, dept, "alert-success");
                            else
                                ShowAlert("Tap-in failed", "", "", "alert-danger");
                        }
                    }
                }
            }
            else // Tap Out
            {
                int rowsAffected = 0;

                using (SqlCommand updateCmd = new SqlCommand("MA_UpdateAttendeeTimeout", conn))
                {
                    updateCmd.CommandType = CommandType.StoredProcedure;

                    updateCmd.Parameters.AddWithValue("@MeetingId", meetingId);
                    updateCmd.Parameters.AddWithValue("@EmployeeNo", strEmpNo);

                    rowsAffected = updateCmd.ExecuteNonQuery();

                    if (rowsAffected > 0)
                        ShowAlert("You have tapped out successfully!", empName, dept, "alert-success");
                    else
                        ShowAlert("Already tapped out or no tap-in found", "", "", "alert-danger");
                }
            }

            // Reset input and focus
            txtCardSerial.Text = "";
            ScriptManager.RegisterStartupScript(this, GetType(), "setFocus",
                $"document.getElementById('{txtCardSerial.ClientID}').focus();", true);
        }
    }
    catch (Exception ex)
    {
        ShowAlert("Error", ex.Message, "", "alert-danger");
    }
}


---

What Changed (Summary)

Before	After

CardSerial used everywhere	EmployeeNo used
EmployeeRecord called but unused	EmployeeRecord validated
@CardSerialNo parameter	@EmployeeNo parameter
No check if EmployeeRecord exists	Stops if employee not found



---

New Flow of Your System

Tap Card
   ↓
EmployeeRecord.cs
   ↓
Get EmployeeNo
   ↓
Check Employee in Meeting DB
   ↓
Tap In / Tap Out

This is better architecture because:

✔ Card number can change
✔ EmployeeNo stays permanent
✔ System is more scalable


---

💡 Since I saw your Meeting Attendance system already uses Stored Procedures and Bootstrap, I can also show you a very useful upgrade used in production attendance systems:

⭐ "Anti Double Tap Protection (2-second lock)"

This prevents card reader double scanning which happens very often.

If you want, I can show you how to add that in 5 lines only.
